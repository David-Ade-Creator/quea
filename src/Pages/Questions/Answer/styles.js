import { createStyles } from "@material-ui/core";

const styles = () =>
  createStyles({
    container: {
      padding: "10px",
    },
    answerProfile: {
      textAlign: "center",
    },
    answercontainer: {
      marginTop: "20px",
    },
    form: {
      marginTop: "5px",
    },
    formbtn: {
      margin: "10px",
    },
    backbtn: {
        marginBottom: "5%",
    }
  });

export default styles;
