import { createStyles } from "@material-ui/styles";

const styles = () =>
    createStyles({
        container:{
            minHeight:'70vh'
        },
        backbtn: {
            marginBottom: "3%",
        },
    });

export default styles;